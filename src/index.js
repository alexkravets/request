'use strict'

const wait        = require('./wait')
const jsonRequest = require('./jsonRequest')
const httpRequest = require('./httpRequest')

module.exports = {
  jsonRequest,
  httpRequest,
  wait
}
